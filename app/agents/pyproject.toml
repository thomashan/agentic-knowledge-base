[project]
name = "agents"
version = "0.1.0"

dependencies = ["pydantic", "crewai", "crewai_tools"]

[tool.setuptools]
packages = ["core", "intelligence", "knowledge", "orchestrator", "planner", "research", "retrieval"]